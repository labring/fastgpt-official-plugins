{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "stream",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "pack": {
      "dependsOn": ["^pack"],
      "outputs": ["*.pkg"]
    },
    "type-check": {},
    "biome-check": {}
  }
}
