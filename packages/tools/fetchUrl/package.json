{
  "name": "fastgpt-tools-fetchUrl",
  "version": "0.0.1",
  "description": "Get the content of a website link and output it in Markdown format, only supports static websites.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @fastgpt-plugin/cli build --minify",
    "build:dev": "npx @fastgpt-plugin/cli build",
    "pack": "npx @fastgpt-plugin/cli pack",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cheerio": "^1.0.0",
    "joplin-turndown-plugin-gfm": "^1.0.12",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/turndown": "^5.0.5",
    "@fastgpt-plugin/cli": "catalog:",
    "@fastgpt-plugin/sdk-factory": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  }
}
