{
  "name": "fastgpt-tools-tavily",
  "version": "0.0.1",
  "description": "Provides Tavily AI search and content extraction capabilities",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @fastgpt-plugin/cli build --minify",
    "build:dev": "npx @fastgpt-plugin/cli build",
    "pack": "npx @fastgpt-plugin/cli pack",
    "test": "vitest run"
  },
  "dependencies": {
    "axios": "^1.13.2"
  },
  "devDependencies": {
    "@fastgpt-plugin/cli": "catalog:",
    "@fastgpt-plugin/sdk-factory": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  }
}
