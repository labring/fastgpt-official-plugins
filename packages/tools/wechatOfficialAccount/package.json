{
  "name": "@fastgpt-plugins/tool-wechat-ofi-account",
  "version": "0.0.1",
  "description": "WeChat Official Account materials, drafts and publishing management tool set",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "npx @fastgpt-plugin/cli build --minify",
    "build:dev": "npx @fastgpt-plugin/cli build",
    "pack": "npx @fastgpt-plugin/cli pack",
    "test": "vitest run"
  },
  "dependencies": {
    "cheerio": "^1.1.2",
    "formdata-node": "^6.0.3",
    "marked": "^12.0.0"
  },
  "devDependencies": {
    "@fastgpt-plugin/cli": "catalog:",
    "@fastgpt-plugin/sdk-factory": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "zod": "catalog:"
  }
}
